# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

**WoWorld (Wonder World)** — 与其说是一个游戏，不如说是在以游戏的方式创建另一个"人世"。程序化生成的体素开放世界RPG，独立开发中。核心卖点：100,000个力求"完整"的NPC——有记忆（上限2000条）、预测、情感、思想、人际关系、欲望、习惯、偏好。

**核心设计哲学**：WoWorld 是一个**故事生成器**，不是一个故事容器。传统RPG（如D&D系列）用开发者手搓的剧情驱动游戏，故事写到哪里游戏寿命就终止在哪里。WoWorld 不预设任何剧情——开发者只定义世界的底层规则（人格参数、行动规则、情绪规律、文化传播率），具体的故事由NPC之间的互动以概率和统计的方式自然涌现。每个存档都是一个完全不同的世界，不同世界之间的文明形态、文化格局、历史轨迹可能天差地别。游戏没有"通关"的概念——只要世界还在运转，故事就永不停歇。

**"人世"的关键维度**：
- **完整的人**：NPC 有记忆、预测、情感、思想、欲望、习惯——与"人"打交道是核心，与"物"打交道是手段
- **历史深度**：玩家进入时世界已运转千百万年，书本和遗迹中保留着先民活过的证据。也可以从零开始建立文明
- **冒险与生活平等**：组建冒险小队去无穷远方探索文明与奇迹，或做个铁匠、商人、政客度过一生——每条路都是"主线"
- **全球多元文化**：西方剑与魔法为基底，世界各地的文化元素（东方、中东、非洲、北欧、美洲、南亚……）自由融入。各文明可自行演化出独一无二的文化特色

**尚无游戏代码**，本仓库纯粹是设计文档。项目当前处于理念设计阶段，无固定的阶段化实现计划。

> ⚠️ **重要说明**：WoWorld 目前仅处于理念设计阶段，具体代码实现层面的技术选型、架构细节、数据结构等均未最终确定。设计文档中提及的任何代码方案、伪代码、数据结构示例等，仅用于辅助阐明设计理念，在实际开发中随时可能大幅重构甚至推翻重写。不应将文档中的实现描述视为已确定的工程规范。

## 文档结构

所有设计文档在 `WoWorld-Design/Happy Game/` 目录下，使用 **Obsidian** 编辑，大量使用 `[[wikilink]]` 交叉引用。文档语言为中文。

### 顶层
- `欢迎.md` — 项目总览与快速导航（hub page）
- `NPC属性.md` — 早期NPC属性头脑风暴

### `想法/` — 概念设计（脑暴阶段，标注 #草稿 的为早期文档）
- `WoW World/` — 世界观核心设计：NPC（心理学/行为/决策/社交）、魔法（19文档完整体系：公理→元素→方程式→类型→魔力→施法→设计→器物→人文→前沿）、战斗、经济、道德、文化、信仰、仪式、事件
- `世界生成/` — 程序化世界生成管线（01-07编号文档 + 自然景观/人文景观子目录）
- `开发想法草稿/` — 技术架构、创意愿景（注：原 `开发流程.md` 阶段性路线图已按 CHG-002 删除）
- `战斗/` — 实时碰撞战斗系统v0.1：武器模块化、体术、团队战、位移、站位

### `开发阶段/` — 实现规格与工程分析
- `NPC活人感模块/` — NPC系统权威规格文档（6000+行，含五层模型、三层LOD、Big Five人格、Plutchik情绪、记忆系统、GOAP决策）
- `家具系统设计.md` — 功能性/辅助/装饰家具分类
- `性能优化分析 20260603.md` — 性能瓶颈与缓解策略
- `开发文档待完善之处与奇思妙想 20260602.md` — 已识别的设计空白（物品数据库、对话系统、天气系统等）
- `修改*.md` — 近期修改日志

### `Change/`（WoWorld-Design 根目录） — 设计文档结构性变更追踪
- 每份变更文档按 `CHG-XXX-简短描述-YYYYMMDD.md` 命名
- 当前包含 CHG-001（NPC数量目标变更）、CHG-002（废止阶段性规划）、CHG-003（设计哲学深化）、CHG-004（创建人世哲学与NPC完整性）、CHG-005（魔法系统冲突修正）、CHG-006（魔法系统模块化重组）
- 详见 `Change/README.md`

### `参考文档/`（WoWorld-Design 根目录） — 核心系统参考性设计文档
- 按 `NNN-简短描述-YYYYMMDD` 格式的子文件夹组织
- 当前批次：
  - `001-核心系统设计参考-20260609/`：涌现叙事、NPC心智、世界生成、全球文化、GOAP决策、体素地形
  - `002-魔法系统冲突分析-20260609/`：魔法文件夹6文档的7项冲突分析与修改建议
  - `003-魔法系统模块化扩展建议-20260609/`：13个建议新建模块、中心文档重构方案、5层实现优先级
- 详见 `参考文档/README.md`

### 文档层级：`想法/` vs `开发阶段/`

| 目录 | 性质 | 标注 | 用途 |
|------|------|------|------|
| `想法/` | 概念设计、脑暴 | `#草稿` = 早期文档 | 探索可能性，允许矛盾、模糊、未决 |
| `开发阶段/` | 实现规格、工程分析 | 版本号（如 ver1.01） | 权威规格，供 Claude Code 开发参考 |

`想法/` 文档之间可能存在冲突或多种方案——这是正常的，因为脑暴阶段在探索可能性空间。`开发阶段/` 文档应保持内部一致性。

### 双版本模式（压缩版）
`开发阶段/NPC活人感模块/` 展示了项目使用的双版本模式：
- **完整版** (`ver1.01.md`): ~6000行，完整阐述
- **压缩版** (`ver1.01short.md`): 保留所有数据结构、关键参数、阈值，供 Claude Code 开发时快速参考

当开发规格文档超过一定规模时，应考虑提供压缩版。

### 关键交叉引用
最重要的"meta文档"是 `想法/世界生成/07-设计整合分析.md`，它对整个设计语料库进行了冲突、缺失、模块和障碍的审查。

## 技术栈（已规划，尚未实现）

- **引擎**: Godot 4.6.2
- **体素**: Voxel Tools (Zylann) 插件 + Transvoxel（Marching Cubes LOD变体）
- **画面**: 3D低多边形 + 像素纹理
- **NPC AI**: GOAP规划（安全网，占9%决策）+ 概率行为树（日常，占90%）+ 可选LLM增强（1%）
- **世界生成**: 多层噪声 → 生物群系 → 聚落 → 建筑(WFC) → 道路 → NPC初始化 → 历史模拟
- **架构**: Game Manager → World Mgr / NPC Mgr → 生成管线 / 模拟管线 → Event Bus → 各子系统
- **美术**: AI生成(Stable Diffusion) + 手动调整(Blender)
- **平台**: Windows / Linux / macOS

## 文件格式

| 格式 | 用途 | 注意事项 |
|------|------|----------|
| `.md` | 所有设计文档（主要格式） | — |
| `.canvas` | Obsidian画布文件（NPC视角/心理模型图） | **二进制JSON，不可当文本编辑** |
| `.svg` | Mermaid导出图表 | — |
| `.png` | 粘贴的图片资产 | — |
| `.base` | Obsidian模板文件 | 用于创建新文档的模板 |

## 文档元数据约定（Frontmatter）

所有设计文档在开头使用 `> ` 块引用格式标注元数据（非 YAML frontmatter）：

```markdown
> **开发代号**: WoWorld (Wonder World)
> **引擎**: Godot 4.6.2
> **开发者**: 独立游戏开发者（Solo）
```

常见属性：引擎、模式（单机/联机）、文档版本、开发者。新文档应遵循此风格。

## 项目配置与工具

- **Obsidian**: `.obsidian/` 目录包含工作区配置，无需手动编辑
- **.gitignore**: `.claude/` 目录已加入 gitignore——Claude Code 自身的产物（settings、worktrees、scheduled tasks）不会提交到仓库

## 工作约定

- 所有新设计文档使用 `.md` 格式，放在对应的 `想法/` 或 `开发阶段/` 子目录下
- 使用 Obsidian wikilink 语法 `[[路径/文件名]]` 引用其他文档
- 新文档跟随现有的 frontmatter 风格（用 `> ` 标注属性，如 `> 独立游戏开发者 · Solo · Godot 4.6.2`）
- 后续Godot项目代码将放在 `woworld/` 目录下（结构见 `技术架构.md`），目前该目录尚不存在
- **设计变更**：对多个文档的结构性修改，在 `Change/` 文件夹中按 `CHG-XXX-简短描述-YYYYMMDD.md` 格式创建变更文档
- **参考文档**：新增参考性资料时，在 `参考文档/` 中创建 `NNN-简短描述-YYYYMMDD` 格式的子文件夹，内部文档从 001 编号
