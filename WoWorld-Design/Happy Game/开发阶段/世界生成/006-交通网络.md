# 006-交通网络

> **状态**: 开发规格 v1.0（v2.0 审核通过——核心内容有效）
> **关联**: [[001-世界生成总流程|001 v2.0]] · [[004-聚落选址与社会结构推导|004 v2.0]] · [[012-权力与经济Bootstrap|012 v2.0]](P11 EconomicRelation 消费交通连接)

---

## 一、总览

交通网络是 P7 阶段。v3 核心变化：
- **保留**陆地 MST 道路算法（逻辑优秀，与尺度无关）
- **新增**海上航线网络——洋流/季风/港口间距驱动的航线生成
- **新增**旅行者 NPC 密度分布

---

## 二、陆地道路网络

### 2.1 最小生成树（MST）算法（保留）

```
节点 = 所有聚落 + 独立建筑（桥梁/哨站/神龛/陵墓）
边权重 = 欧氏距离 × 地形代价 × 0.7 + 已有道路利用 × 0.3
```

**地形通行代价**：

| 地形 | 代价系数 |
|------|---------|
| 平原 | 1.0 |
| 丘陵 | 1.5 |
| 森林 | 2.0 |
| 沼泽 | 4.0 |
| 山脉 | 10.0 |
| 水域 | ∞（不可通行——除非有桥） |

### 2.2 道路等级

| 等级 | 宽度 | 路面 | 连接对象 |
|------|------|------|---------|
| **主干道（驿道）** | 4-6m | 石板 | 城市↔城市 / 城市↔城堡 |
| **次级道路** | 2-3m | 碎石 | 城市↔小镇 / 小镇↔主干道 |
| **小路** | 1-1.5m | 土路 | 小镇↔村庄 / 村庄↔次级道路 |

### 2.3 桥梁生成

道路跨越水域时自动生成桥梁：
- 桥宽 = 道路宽度
- 材料 = 区域建材（石桥→山区；木桥→森林；砖桥→发达聚落）
- 大型桥梁（跨度>20m）仅在 3 级以上道路上生成

---

## 三、海上航线网络（新增）

### 3.1 航线节点与边权重

```
节点 = 港口城市 + 可停靠的岛屿聚落
边权重 = f(地理距离, 洋流助力/阻力, 季风方向, 中途岛屿补给, 暗礁/浅滩危险)
```

```rust
struct SeaRoute {
    from_port: SettlementId,
    to_port: SettlementId,
    /// 航线路径——考虑洋流修正的实际航行路径
    waypoints: Vec<Vec2>,
    /// 航行时间估算（游戏时间——天）
    travel_time_days: f32,
    /// 航线等级
    grade: SeaRouteGrade,
    /// 季节性变化——某些航线仅在特定季节可安全航行
    seasonality: Option<SeasonalRestriction>,
}

enum SeaRouteGrade {
    Coastal,      // 近海航线——沿海岸线，安全，短距
    CrossSea,     // 跨海航线——横跨海域，中等风险和距离
    Oceanic,      // 远洋航线——跨越大洋，高风险，最长距离
}
```

### 3.2 航线时间计算

> ★ 经 [[参考文档/第二部分-模块系统基础扩张存档-20260618/023-载具系统设计探讨-20260615/设计草稿/001-总纲与核心概念|载具系统设计]] 确认——
> 航速由载具系统的 `VehicleDef.base_speed_ms` 定义，日常不受洋流/季风影响。
> 航线时间 = 距离 / 航速。仅灾害级天气施加减速。

```rust
/// 航线航行时间——简化版
/// 航速从载具系统查询，不再依赖洋流/季风
fn compute_sailing_time(
    from: Vec2,
    to: Vec2,
    vehicle_def: &VehicleDef,  // ← 从载具系统获取
) -> f32 {
    let distance_km = from.distance(to) / 1000.0;
    // 航速 = 载具类型的基准航速（m/s）→ 转换为 km/h
    let speed_kmh = vehicle_def.geometry.base_speed_ms * 3.6;
    // 航线时间（游戏小时）= 距离 / 航速
    distance_km / speed_kmh
}
```

### 3.3 航线分级

| 等级 | 覆盖范围 | 航行时间 | 风险 | 典型船只 |
|------|---------|---------|------|---------|
| **近海航线** | <50km，沿大陆海岸 | 数小时-1天 | 低 | 小帆船、渔船 |
| **跨海航线** | 50-300km，跨海域 | 1-7天 | 中（风暴、海盗） | 大帆船、商船 |
| **远洋航线** | 300km+，跨越大洋 | 数周-数月 | 高（风暴、迷航、补给不足） | 大型远洋船 |

---

## 四、旅行者 NPC（新增）

在已有道路和航线上动态分布旅行者（v3.0 §8.3）。

```rust
fn traveler_density(road: &Road, region: &RegionState) -> f32 {
    let base = match road.grade {
        RoadGrade::Primary => 0.05,   // 每 km 0.05 人
        RoadGrade::Secondary => 0.02,
        RoadGrade::Tertiary => 0.005,
    };

    let safety_mod = region.lawfulness * 1.5;      // 治安好 → 更多旅行者
    let prosperity_mod = region.economic_prosperity * 1.2; // 繁荣 → 更多商旅

    base * safety_mod * prosperity_mod
}

enum TravelerType {
    Merchant,       // 商队（3-8 NPC + 马车/驮兽）
    Courier,        // 信使（1-2 NPC，快速移动）
    Pilgrim,        // 朝圣者（1-5 NPC）
    Wanderer,       // 流浪者（1 NPC）
    Adventurer,     // 冒险小队（2-6 NPC）
    Bandit,         // 盗贼（由低治安+经济崩溃涌现）
    Refugee,        // 难民（战争/灾害驱动）
    Patrol,         // 巡逻队（治安维持）
}
```

---

## 五、关键数据结构

```rust
struct RoadNetwork {
    edges: Vec<RoadEdge>,
    bridges: Vec<Bridge>,
}

struct RoadEdge {
    from: SettlementId,
    to: SettlementId,
    grade: RoadGrade,
    path: Vec<Vec2>,           // 路径节点
    total_length: f32,         // 总长度 (km)
    travel_time_walking: f32,  // 步行时间 (游戏小时)
}

struct TransportNetwork {
    roads: RoadNetwork,
    sea_routes: Vec<SeaRoute>,
}

/// 查询任意两点之间的最优路线
fn find_route(
    from: Vec2,
    to: Vec2,
    network: &TransportNetwork,
    mode: TravelMode,
) -> Option<Route> { /* ... */ }
```

---

> **关联文档**：[[文件备份/004-聚落生成]] · [[002-自然景观]] §五 海洋系统
