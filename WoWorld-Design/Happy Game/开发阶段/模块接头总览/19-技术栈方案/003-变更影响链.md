# 技术栈方案 — 变更影响链

> **最后验证**: 2026-06-18 | **源文档版本**: 技术栈 v4.0 (CHG-038 审计升级)
>
> 技术栈方案是全局架构文档——其变更影响全部21个模块。

## 引擎/语言版本变更
→ **全部模块**: Rust stable版本 / Godot 4.6 LTS → 所有GDExtension接口
→ **构建系统**: 编译目标/feature flag
→ **Mod系统**: TOML schema版本

## 架构拓扑变更
→ **全部模块**: Rust模拟核心 → GDExtension → Godot客户端 三层架构
→ **新增/移除层**: 所有模块的crate归属变更
→ **woworld_core 边界**: 核心类型提升/降级影响所有消费方

## 性能预算变更 ⚠️ 最高影响
→ **全部模块**: 帧预算16.7ms / VRAM 4.2GB / 内存1.4GB
→ **峰值互斥原则**: 各子系统预算重新分配 → 可能限制某些模块功能
→ **具体预算变更影响**:
  - CPU动画 ≤0.5ms → NPC数量/动画复杂度
  - Rust模拟 ≤4ms/帧 → 所有模拟系统 (GOAP/经济/物理/天气)
  - 音频 ≤0.17ms → 同时发声数/传播精度
  - 天气 ≤0.25ms → 天气轮询精度
  - 降水粒子 ≤0.4ms GPU → 粒子数量

## LOD 统一架构变更 (CHG-049 全面深化 ★2026-06-20)
→ **全部模块**: 场景8层(0-7, 12km) × 角色5层(0-4, 800m+) 统一距离带 ← CHG-049 §二
→ **LODCoordinator**: 8步冲突解决算法 (基础→硬约束→级联→Attention→VRAM降→帧降→Clamp→迟滞) ← CHG-049 §五
→ **全部模块**: 7维LodPrescription (audio_lod★4→5档) + 7条跨维硬约束 ← CHG-049 §三+§四
→ **NPC**: L1全AI→L2简化→L3统计→L4仅存在。ai_lod 5档精确契约 ← CHG-049 §三
→ **NPC**: 级联交互——InteractionIntent 7变体+预算制拉升(≤0.3ms) ← CHG-049 §六
→ **音频**: audio_lod 5档+CommunicationIntent(5级)+AcousticProjection+AudioBroadcast广播+失真链→谣言 ← CHG-049 §七
→ **感知**: L1全入→L2主要实体→L3聚合 (CHG-031 已定义, LOD层级只是调度分桶)
→ **物理**: L1全精度→L2简化→L3无碰撞 (CHG-042 已定义) ← CHG-049 §三
→ **体素**: 8级Clipmap LOD, 3km Signed Heightfield ← CHG-049 §二(场景线)
→ **建筑**: BuildingLOD→scene_lod 3级映射+Landmark+1规则+仿真LOD独立 (CHG-043 TBD解决) ← CHG-049 §九
→ **植被**: VegetationProvider 消费 scene_lod, VMC内部4级自治 ← CHG-049 §十
→ **VRAM**: VramLedger Rust侧记账+3级阈值 70/85/95% ← CHG-049 §十二
→ **过渡**: 7维分表时序(升级/降级延迟) ← CHG-049 §八
→ **生命周期**: L1-L4↔ai_lod 0-4映射, 统一时间流速 ← CHG-049 §十一
→ **玩家**: 永远LOD0+专注聆听(Alt键)视线锥内audio_lod-1 ← CHG-049 §七

## 技术栈方案自身格式变更
→ **TDI编号体系**: 237+126 TDI → 新增/重新编号影响引用
→ **CHG-038**: 技术栈全量审计 → 审计结论归档
→ **CHG-033**: 物理方案改为"仅玩家PhysicsServer3D" → 技术栈方案 §物理 完全重写

## 渲染管线变更
→ **Godot shader**: TABS cel (单diffuse, 无PBR) → 所有角色/建筑/道具 shader
→ **体素**: Transvoxel → ArrayMesh → Godot渲染
→ **天空/海洋**: 体积云shader / Gerstner波shader → 天气/海洋模块

## 数据存储变更
→ **LMDB → 替代方案**: 所有Chunk存储/NPC存档/历史记录
→ **双层记忆架构**: NPC记忆(热) + LMDB(冷)

## 平台支持变更
→ **Windows/Linux/macOS**: 各平台的渲染后端/文件系统/线程模型
→ **硬件目标**: GTX 1660 SUPER 6GB → 最低配置变更
