# 08-NPC行动涌现与分类 — NPC 认知 v1.1 传播审计

> **审计日期**: 2026-06-22
> **来源变更**: CHG-057 (NPC 认知 v1.1) + CHG-058 (自审修正)
> **断裂程度**: ★★★ 严重 — 接口缺口（AgentSnapshot 缺失出口条目）、核心机制（两阶段审议）未传播到文档

---

## 1. 过时引用

### 1.1 版本号未更新 — 2 处
- `README.md:6` → `v1.0` 应更新为 `v1.1`
- `001-NPC行动涌现总纲.md:5` → 内容已含 CognitiveTide 3 字段但版本号仍为 v1.0

### 1.2 CognitiveTide 字段名称不一致 — 2 处
- `模块接头总览/02-NPC活人感/001-接口出口.md:77` → `mental_quiet` 应为 `mind_quietude`
- `06-认知与智慧系统/006-创新管线与跨领域对接.md:96` → `npc.cognitive_tide.mental_quietude` 应为 `npc.cognition_tide.mind_quietude`

### 1.3 ActionCandidate 结构定义过时
- `010-跨模块接口与数据合同.md:119` → 6 字段，应为 8 字段（新增 `prediction: Option<OutcomePrediction>` 和 `mental_model_modulation: f32`）

---

## 2. 缺失概念

### 2.1 predict_outcome / 两阶段延迟审议 — 3 处缺失
- `001-NPC行动涌现总纲.md` GOAP 规划器描述缺失 deliberation 机制
- `010-跨模块接口与数据合同.md` 消费侧表缺失 `predict_outcome()` 和 `deliberation_depth()`
- `README.md` 概念速查表未提及 deliberation

### 2.2 CognitiveTide 影响执行噪声 — 2 处缺失
- `006-技能精度与原子执行噪声.md` `effective_noise()` 函数只考虑 skill_level 和 intoxication，不含 CognitiveTide 3 字段
- 001 总纲中 GOAP 效用折扣效应未描述

### 2.3 接口文档缺失 CognitiveTide 消费关系
- `010-跨模块接口与数据合同.md` 消费侧表(1.2)未列出 CognitiveTide

---

## 3. 接口不一致

### 3.1 出口文件 — AgentSnapshot 完全缺失（最严重缺口）
`02-NPC活人感/001-接口出口.md` 列出了 23 个导出条目但**零提及 AgentSnapshot**——这是 08 模块定义的、消费方模块最常引用的核心概念。

### 3.2 入口文件 — 缺失 3 个关键导入
`02-NPC活人感/002-接口入口.md` 缺失：CognitiveTide、predict_outcome()、deliberation_depth()

### 3.3 CognitiveTide 字段名不一致
`mental_quiet`（接头文件）vs `mind_quietude`（08/06 文档）——消费方会引用错误字段名。

### 3.4 ActionCandidate 字段列表不完整
010 合同文件声明 6 字段，实际应为 8 字段。

---

## 4. 新上游需求

### 4.1 对 06-认知与智慧系统的需求
- `predict_outcome()` 和 `deliberation_depth()` 必须为 pub API
- CognitiveTide 更新周期需与 08 决策周期对齐

### 4.2 对 GOAP 规划器的需求
- 消费 `ActionCandidate.prediction` 进行权重调制
- 调用 `deliberation_depth()` 决定是否触发审议
- `survival_suppression` sigmoid 压制（max_urgency > 0.7 时跳过审议）

### 4.3 对 06-认知命名一致性的需求
- 修复 `006-创新管线与跨领域对接.md:96` 的双重命名错误

---

## 5. 修复计划

### 优先级 1 — 接口缺口
| 文件 | 修改 |
|------|------|
| `02-NPC活人感/001-接口出口.md` | 新增 AgentSnapshot 导出条目（5 段 26 字段 v1.1）；修正 mental_quiet→mind_quietude |
| `02-NPC活人感/002-接口入口.md` | 新增 3 个条目：CognitiveTide、predict_outcome()、deliberation_depth() 从 06-认知 |

### 优先级 2 — 概念传播
| 文件 | 修改 |
|------|------|
| `08-.../001-NPC行动涌现总纲.md` | v1.0→v1.1；新增 §二.6 两阶段审议；GOAP 效用折扣→CognitiveTide 调制 |
| `08-.../010-跨模块接口与数据合同.md` | 消费侧表新增 CognitiveTide/predict_outcome/deliberation_depth；ActionCandidate 6→8 字段 |
| `08-.../006-技能精度与原子执行噪声.md` | 新增 CognitiveTide 三维调制公式到 effective_noise() |

### 优先级 3 — 命名一致性
| `08-.../README.md` | v1.0→v1.1 |
| `06-.../006-...md:96` | cognitive_tide.mental_quietude→cognition_tide.mind_quietude |

### 优先级 4 — 加强已有引用
- README.md 子文档索引补充 v1.1/v1.2 特性标注
- 003 补充修订记录
- 000-变更日志 新增 CHG-057/058 条目
