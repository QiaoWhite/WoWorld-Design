# 003 — 待验证与开放问题

> **父文档**: [[README]]
> **日期**: 2026-07-09
> **用途**: 这些项**不进开发阶段权威规格**，直到原型验证 / 依赖模块成熟 / 用户拍板。审计循环产出的"无法自主解决、需用户审阅"的问题也汇总于此。

---

## 一、需原型验证（不能纸上拍死）

| # | 项 | 失败模式 | 候选方案（按序试） | 关联 |
|---|----|---------|------------------|------|
| V1 | **玩家站在旋转甲板/移动平台** | `move_and_slide` 在旋转父级上抖/往低侧滑 | ①平台速度+手动偏航修正 ②reparent 玩家节点到船节点下（防全局变换重置抖动）③每帧向下 shapecast 吸附局部甲板 | Q6 |
| V2 | **玩家攀爬** move_and_slide 在竖直/悬垂面 | 重力/floor 检测假设"上"导致贴不住 | 攀爬模式关重力+关 floor-snap，墙面切平面自由移动（Godot 攀爬教程标准套路，风险低于 V1） | Q8 |
| V3 | **Godot 4.x 变换插值** | Rust 固定 tick 位姿在渲染帧率上抖 | 开 `physics_interpolation`；或桥接层插值 | Q6 |

> 遵项目"先收集数据再形成假说"调试铁律：这三项应在原型中用实测（含 RenderDoc/帧检查）定夺，不在文档层锁定具体实现。

## 二、待定数值（依赖调参/原型，不写进权威规格）

| # | 项 | 说明 |
|---|----|------|
| N1 | 冲量分流系数（穿透 vs 击退） | 每武器 TOML；具体值需战斗手感调参 |
| N2 | 握力/体力曲线 | 攀爬体力衰减率、材质×湿度→握力映射曲线 |
| N3 | 重力常数 / 阻力 / 浮力项系数 | COM 积分已有 9.81；投射物 drag、浮力项系数待调 |
| N4 | 三级着地阈值统一 | 现有两套（>10m/>30m 距离 vs >8/>20 m/s 速度）差约 3 倍，需统一为**速度制单一真相源**并重调数值（见下 Q-A1） |
| N5 | 二次冲量封顶值 | Q5 旁观者踉跄的二次小冲量上限 |

## 三、依赖不成熟模块的插口（现在不耦合，留字段）

用户明确提示：**魔法、战斗模块尚不成熟**。以下按"地基暴露插口、消费方随模块成熟再填"处理，现在不写消费方数值：

| # | 插口 | 地基侧（现在做） | 消费方（待模块成熟） |
|---|------|----------------|-------------------|
| P1 | 火球/法术 → 冲量 | `SpatialEvent::Explosion`/径向 `Impulse` 总线已在 | 魔法定义"某法术发多大冲量/半径衰减" |
| P2 | 招式 → 击退系数 | `AttackTrajectory.launch_power` 重解释为传递系数 | 战斗定义每招系数 |
| P3 | 魔法"蜘蛛攀爬术" | `Climbing{max_grip_angle,gravity_frame}` 可被临时改写 | 魔法 buff 组件拨开关 |
| P4 | 战斗"飞檐走壁"招式 | 同 `Climbing` 临时授予 | 战斗招式接入 |
| P5 | 投射物 payload | `Projectile.payload: HitPayload` 字段留位 | 战斗/魔法填伤害/效果内容 |

## 四、集中待用户审批的问题（审计循环产出）

> 审计循环（任务 #5）中"能解决的已解决"，无法自主解决、需用户拍板的集中于此。

| # | 问题 | 现状/建议 | 裁决（2026-07-09 已审批） |
|---|------|----------|---------|
| Q-A1 | **三级着地阈值该以哪套为单一真相源？** | 建议：速度制（>8/>20 m/s，COM 积分原生输出，同时覆盖撞墙水平冲击），距离阈值作为它的涌现推导；但战斗 009 的 >10m/>30m 需相应重调（10m→14m/s、30m→24.3m/s，与 8/20 不完全吻合） | ✅ **采速度制为单一真相源**；战斗 009 距离阈值降级为涌现推导，数值待重调（N4）。已同步 战斗/009 + 运动学地基/001 + CHG-067 |
| Q-A2 | **首冲刺是否现在动工？** | Q2-Q5 主链 + Mass 组件是可建首冲刺；也可只文档暂不编码 | ✅ **只留文档，暂不编码** |
| Q-A3 | **新子模块命名** | 采用 `模型动作与物理系统/运动学地基/`；也可并入 `空间查询与物理/` | ✅ **认可 `运动学地基/`** |
| Q-A4 | **攀爬导航图工作量归属** | Q8 修正指出"可攀爬边"属导航/NPC 行动系统，非物理模块；需在那边立项 | ✅ **登记为待立项**，待攀爬进入冲刺再实做 |

## 五、明确排除（本轮已决定不做，记录以防重开）

- 通用刚体物理引擎（Rapier/Jolt/Avian）——Q1。
- 大规模涌现式刚体动力学（堆叠/碎块崩落/货物乱滚）——Q1.5。
- N 体动量守恒碰撞求解——Q5。
- 真实流体/液体仿真、真实地面摩擦 μ 滑行、材料热胀冷缩——盘点为空白且不补（除非未来专门立项）。
- nalgebra/parry 引入——glam 足够，且地形是 SDF（parry 帮不上）。

## 六、集成留白（首冲刺敲定，非用户审批项）

审计循环（任务 #5）中自行修正/敲定、无需用户拍板、但需在编码首冲刺落实的实现细节：

| # | 项 | 结论 |
|---|----|------|
| I1 | **定向冲量投递机制** | 审计修正：不复用 `SpatialEventBus`（其 `WeaponImpact` 只带标量力+位置，无目标/无冲量向量）。改用**专用 `ImpulseQueue`**（确定性 drain）；感知通知另走 `SpatialEventBus`。已同步 001/002/CHG-067 |
| I2 | **COM 速度单一真相源** | 现有积分器在 `AnimationBodyState.com_velocity` 上跑，ECS 有独立 `Velocity` 组件（死代码）。建议 `Velocity` 为权威、`com_velocity` 为每帧镜像；首冲刺敲定桥接 |
| I3 | **碰撞候选取用** | 伪代码用 `entities_in_aabb(...).first()`（现有 trait 方法），非臆造 API |
