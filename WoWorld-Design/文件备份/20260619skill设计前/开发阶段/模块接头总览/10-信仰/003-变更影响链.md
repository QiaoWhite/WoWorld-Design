# 信仰模块 — 变更影响链

> **最后验证**: 2026-06-18 | **源文档版本**: 信仰系统 v1.0 (CHG-025)

## 影响链总览

```
信仰系统变更
├── FaithId 编码格式变更
│   ├── → NPC: 信仰归属存储
│   ├── → 历史: 宗教事件溯源
│   ├── → 文化: 文化-信仰关联
│   └── → 权力: 神权政体引用
├── FaithTheology 参数增删
│   ├── → 文化: 技术派生
│   ├── → 世界生成: SacredArchitectureParams 重新派生
│   ├── → 魔法: MagicReligionRelation 改变
│   └── → UI: 信仰类型标签
├── FaithDefinition 结构变更
│   └── → 世界生成: P2.5 信仰生成管线
├── ReligiousPracticeProfile 字段变更
│   ├── → NPC: 热路径存储和读取
│   ├── → 战斗: 士气加成
│   └── → 权力: 神职层级派生
├── FaithQuery trait 方法增删
│   └── → 全部消费模块适配
├── FaithMut trait 方法增删
│   └── → 世界生成 / 历史模拟引擎
├── ReligiousReproductionNorms 变更
│   ├── → 生命: 繁衍系统
│   └── → NPC: 信仰继承
├── 5 通道传播模型变更
│   ├── → NPC: NPC 间信仰传播速率
│   └── → 文化: 文化-信仰互动
├── FaithCalendarQuery trait 变更
│   └── → 文化: 节日系统
├── FaithAgent trait 变更
│   ├── → NPC: trait 实现适配
│   └── → Player: trait 实现适配
├── MagicReligionRelation 变更
│   ├── → 文化: 技术派生
│   └── → 魔法: 社会接受度
├── DivineAuthorizationEvent 变更
│   └── → 权力: PowerEdge 创建逻辑
├── SacredArchitectureParams 变更
│   └── → 世界生成: 建筑生成
├── FuneralCustoms 变更
│   ├── → 生命: DeathCustom
│   └── → 历史: 墓碑
├── FaithGenealogy 变更
│   ├── → 历史: 事件因果链
│   └── → UI
├── faith_morale_bonus() 变更
│   └── → 战斗: TeamMorale
├── ChildFaithProfile 变更
│   └── → 生命: 繁衍系统
└── 实践优先模型变更
    ├── → NPC: 废除 piety 标量
    └── → 全部: DerivedReligiosity 三维度消费
```

## 变更风险等级

| 变更范围 | 风险等级 | 影响模块数 | 备注 |
|---------|---------|-----------|------|
| FaithId 编码格式 | 🔴 高 | 4+ | 全局标识，需数据迁移 |
| FaithTheology 参数增删 | 🔴 高 | 4 | 核心原子，所有派生特征受影响 |
| ReligiousPracticeProfile 字段 | 🟡 中 | 3 | ~40B/NPC，存储格式兼容性 |
| FaithQuery 新增方法 | 🟢 低 | N | 默认实现可得 |
| 传播模型参数 | 🟢 低 | 2 | [TUNING] 参数 |
| FaithAgent trait 新增方法 | 🟡 中 | 2 | NPC + Player 实现适配 |
| FuneralCustoms 类型扩展 | 🟢 低 | 2 | 枚举扩展 |

## 所有权迁移记录

| 概念 | 原 Owner | 新 Owner | 迁移 CHG | 状态 |
|------|---------|---------|----------|------|
| ReligiousReproductionNorms | Life 012 | 信仰系统 002 | CHG-025 | ✅ 已完成 |

## 关联 CHG 影响链

| CHG | 变更内容 | 影响 | 状态 |
|-----|---------|------|------|
| CHG-025 | 信仰系统 v1.0 创建 | 建立全部接口 + 一项所有权迁移 | ✅ 已闭合 |
| CHG-024 | FestivalCalendarQuery 模式 | 信仰系统实现文化定义的 trait | ✅ 已闭合 |
| CHG-023 | DivineAuthorization→权力 | 信仰事件驱动权力边创建 | ✅ 已闭合 |
