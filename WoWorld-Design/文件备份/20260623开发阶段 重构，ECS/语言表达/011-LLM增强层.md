# 011-LLM增强层

> **开发代号**: WoWorld (Wonder World)
> **引擎**: Godot 4.6.2
> **开发者**: 独立游戏开发者（Solo）
> **版本**: v2.0
> **状态**: 开发规格

---

## 一、定位

LLM 增强层是语言表达模块的**可选装饰层**——包裹现有模板管道，在玩家启用时提供 LLM 增强的输出，在未启用或不可用时无缝回退到模板。它不修改模板管道的任何代码，不绑定任何特定 LLM 来源。

核心原则：
- **独立于模板系统**——模板管道永远是 fallback，LLM 是锦上花
- **场景粒度开关**——玩家按 19 个场景自由选择是否使用 LLM
- **后端无关**——本地部署（ollama/llama.cpp/vLLM）和云端 API（OpenAI/Anthropic/Groq）统一抽象
- **NPC 自主决定深度**——不由系统设信任硬阈值，NPC 个性/情绪/文化自主决定对话意愿

---

## 二、架构

```
llm_enhancement/              ← 只依赖 lang_expression 和 woworld_types
├── orchestrator.rs           — LlmOrchestrator (统一入口)
├── client.rs                 — LlmClient, LlmRateLimiter, LlmPriority
├── backend.rs                — LlmBackend trait, LlmBackendRegistry
├── safety.rs                 — SafetyGateway
├── budget.rs                 — TokenBudget
├── cache.rs                  — ResponseCache
├── prompt.rs                 — PromptLibrary, PersonalityPromptBuilder
├── engines/
│   ├── dialogue.rs           — LlmDialogueEngine
│   ├── text.rs               — LlmTextEngine
│   ├── narrative.rs          — LlmNarrativeEngine
│   ├── character.rs          — LlmCharacterEngine
│   └── world.rs              — LlmWorldEngine
└── config.rs                 — LlmSceneConfig
```

### 依赖方向

```
woworld_types ← lang_expression ← llm_enhancement
llm_enhancement → 不依赖任何业务模块
主循环/集成层 → 组装 llm_enhancement 并注入 lang_expression
```

---

## 三、场景粒度开关

### 3.1 LlmSceneConfig

```rust
pub struct LlmSceneConfig {
    // ── 对话场景 ──
    pub deep_dialogue: bool,          // 深度对话
    pub normal_dialogue: bool,        // 普通对话
    pub combat_dialogue: bool,        // 战斗对话
    pub phatic_greetings: bool,       // 快速问候/告别
    pub npc_to_npc_dialogue: bool,    // NPC 间对话
    pub travel_companion_chat: bool,  // 旅伴对话
    pub multi_party_dialogue: bool,   // 多人对话 (3+)

    // ── 文本场景 ──
    pub book_text_polish: bool,       // 书籍/卷轴润色
    pub inscription_text: bool,       // 铭文/碑文
    pub letter_writing: bool,         // NPC 写信

    // ── 叙事场景 ──
    pub journal_narrative: bool,      // 大日志叙事化
    pub battle_narrative: bool,       // 战斗叙事
    pub historical_retelling: bool,   // 历史口述

    // ── 角色场景 ──
    pub npc_background_story: bool,   // NPC 背景故事
    pub npc_diary: bool,              // NPC 日记
    pub bard_composition: bool,       // 吟游诗歌

    // ── 世界场景 ──
    pub tree_reading: bool,           // 读树
    pub ruin_description: bool,       // 遗迹探索
    pub weather_atmosphere: bool,     // 天气描写
    pub item_history: bool,           // 物品历史

    // ── 全局 ──
    pub master_switch: bool,          // 总开关
}
```

---

## 四、LLM 后端抽象

### 4.1 LlmBackend trait

```rust
pub trait LlmBackend: Send + Sync {
    fn backend_id(&self) -> &str;
    fn backend_type(&self) -> LlmBackendType;
    fn is_available(&self) -> bool;
    fn max_context_tokens(&self) -> u32;
    fn call(&self, request: &LlmRequest) -> Result<LlmResponse, LlmError>;
    fn call_streaming(&self, request: &LlmRequest, on_token: Box<dyn Fn(String)+Send>) -> Result<(), LlmError>;
    fn estimated_latency_ms(&self, prompt_tokens: u32) -> u32;
    fn estimated_cost(&self, prompt_tokens: u32, completion_tokens: u32) -> f32;
    fn health_check(&self) -> Result<(), LlmError>;
}
```

### 4.2 LlmBackendType——本地5 + 云端6 + Mock

```rust
pub enum LlmBackendType {
    // 本地部署
    LocalOllama,              // ollama serve
    LocalLlamaCpp,            // llama.cpp server
    LocalVllm,                // vLLM
    LocalTextGenWebui,        // text-generation-webui
    LocalOpenAiCompatible,    // 通用 OpenAI 兼容本地服务
    // 云端 API
    CloudOpenAi,              // api.openai.com
    CloudAnthropic,           // api.anthropic.com
    CloudGroq,                // api.groq.com
    CloudTogether,            // api.together.xyz
    CloudOpenRouter,          // openrouter.ai
    CloudCustom,              // 自定义兼容服务
    // 特殊
    Mock,                     // 测试 mock
}
```

### 4.3 LlmBackendConfig + LlmMultiBackendConfig——玩家配置

```rust
pub struct LlmBackendConfig {
    pub backend_type: LlmBackendType,
    pub endpoint_url: String,              // "http://localhost:11434" or "https://api.openai.com/v1"
    pub api_key: Option<String>,           // None for local
    pub model_name: String,                // "llama3:8b" or "gpt-4o"
    pub max_context_override: Option<u32>,
    pub timeout_seconds: u32,
    pub streaming: bool,
    pub usage_label: String,               // "主力模型" / "快速模型" / "本地备用"
}

pub struct LlmMultiBackendConfig {
    pub backends: Vec<LlmBackendConfig>,
    pub scene_assignments: HashMap<String, String>,  // scene_tag → backend usage_label
    pub default_backend: String,
}
// 示例: 主力模型=CloudOpenAi(gpt-4o)用于深度对话/背景故事,
//       快速模型=LocalOllama(llama3:8b)用于普通对话/旅伴闲聊,
//       本地备用=LocalLlamaCpp(mistral:7b)用于故障转移
```

### 4.4 LlmBackendRegistry——多后端管理+故障转移

```rust
pub struct LlmBackendRegistry {
    backends: HashMap<String, Box<dyn LlmBackend>>,
    health_status: HashMap<String, BackendHealth>,
    scene_routing: HashMap<String, String>,  // scene → backend_id
    default_backend_id: String,
}

impl LlmBackendRegistry {
    pub fn select_backend(&self, scene_tag: &str) -> Result<&dyn LlmBackend, LlmError> {
        // ① 场景路由 → ② 默认后端 → ③ 故障转移(第一个健康的)
    }
}
```

### 4.4 LlmRequest / LlmResponse——通用格式

```rust
pub struct LlmRequest {
    pub system_prompt: String,
    pub user_prompt: String,
    pub max_tokens: u32, pub temperature: f32,
    pub response_format: Option<ResponseFormat>,
    pub scene_tag: String,
}
pub struct LlmResponse { pub text: String, pub tokens_used: TokenUsage, pub latency_ms: u32, pub backend_id: String }
pub enum LlmError { NetworkError(String), Timeout, RateLimited{..}, AuthError, ContextTooLong{..}, BackendUnavailable, InvalidResponse(String), SafetyRefused(String) }
```

---

## 五、NPC 自主对话意愿

### 5.1 NpcDialogueWillingness

系统不设信任硬阈值。NPC 通过六维模型自主决定是否愿意深度对话：

```rust
pub struct NpcDialogueWillingness {
    pub overall: f32,  // 0-1
    pub breakdown: WillingnessBreakdown,
}
pub struct WillingnessBreakdown {
    pub personality_contribution: f32,    // openness×0.30 + extraversion×0.20 + agreeableness×0.10
    pub mood_contribution: f32,           // 平静=高意愿，极端情绪=只想发泄
    pub relationship_contribution: f32,   // trust×0.20——连续值，非阈值
    pub topic_contribution: f32,          // 亲身经历/专业领域=高兴趣
    pub situation_contribution: f32,      // 旅途中+0.10，危险中-0.20
    pub culture_contribution: f32,        // 直接性+表达性
}
```

willingness 不决定"是否调用 LLM"——它决定 LLM 生成什么样的回应：
- `>0.7`: "Elaborate freely, share personal stories."
- `>0.4`: "Engage but don't overshare."
- `>0.15`: "Keep responses short and guarded."
- `<0.15`: 不调用 LLM——模板足够

### 5.2 PersonalityPromptBuilder

```rust
impl PersonalityPromptBuilder {
    pub fn build_npc_persona(&self, npc: &NpcSnapshot) -> String {
        // Big Five + 职业年限 + 当前情绪 + 说话风格规则 + 文化规范 + 关系总结 + 相关记忆
        // 输出为 LLM system prompt 的一段
    }
    fn derive_speaking_rules(&self, npc: &NpcSnapshot) -> String {
        // agreeableness<0.3 → "Be terse and blunt."
        // extraversion>0.7 → "Be talkative and expressive."
        // openness<0.3 → "Be practical and concrete."
        // culture.directness<0.3 → "Be indirect. Never criticize directly."
        // culture.silence_tolerance>0.7 → "Silence is normal. Don't rush."
    }
}
```

---

## 六、旅伴对话

### 6.1 多人发言权

```rust
pub fn multi_travel_turn(
    participants: &[Participant],
    shared_activity: SharedActivity,
    seconds_since_last_anyone_spoke: f32,
    environment: &EnvironmentObservations,
    active_rumors: &[PropagationChain],
) -> Option<(EntityId, ConversationTopic, TravelUtteranceStyle)> {
    // 为每个参与者计算 SpeakUrge:
    //   环境触发(景色) 0.4 + 时事触发(流言) 0.35
    //   + 时间触发(沉默太久) max 0.3 + 内在触发(随机 2%) 0.25
    // 选最高冲动者发言
}
```

### 6.2 话题来源: 景色 + 时事

- **景色**: 地标(个人/历史/美学/实用 4级连接)、天象(日落/彩虹/风暴)、野生动物(猎人/博物学者高兴趣)、季节变化
- **时事**: 信息传播系统 `InformationPropagationQuery` → 活跃流言（<3天）→ NPC听说过的 → impact>0.15 → 按兴趣过滤(熟人/职业/家乡/开放性)

---

## 七、灵活可闻半径

> ⚠️ **[CHG-030 权威注解]**: effective_audible_radius() 的声学公式定义位于音频模块。本处为消费侧引用。

```rust
pub fn effective_audible_radius(
    base_radius: f32,
    speaker: &NpcSnapshot,
    environment: &EnvironmentContext,
    intentional_mode: &ConversationMode,
) -> f32 {
    base_radius
        * intentional_factor   // Normal=1.0, Loud=1.5, Whisper=0.15, Conspiracy=0.08
        * env_noise_factor     // Silent=1.5, Market=0.5
        * terrain_factor       // OpenPlain=1.3, Forest=0.7, Canyon=1.5
        * weather_factor       // 雨×0.8, 风×0.7, 雷×0.5
        * culture_factor       // 1.0 + personal_space×0.1
        * personality_factor   // 1.0 + (extraversion-0.5)×0.3
}
```

---

## 八、沉默模型

沉默是有效的交流。`SilenceTracker` 基于文化沉默容忍度 + 上一轮话语类型（问题/陈述/情感/应酬/命令/自言自语）决定沉默含义：

| 状态 | NPC 反应 |
|------|---------|
| Normal | 不说话——沉默正常 |
| Prolonged | 外向 NPC 可能主动打破 |
| Awkward | 高友善→温和打破；低友善→质问 |
| ConversationEnded | 自然结束→Closing Phatic |

---

## 九、NPC 间对话 LLM 策略

```rust
pub enum LlmPriority {
    PlayerInteractive = 0,      // 玩家主动对话——最高优先
    PlayerTravelCompanion = 1,  // 旅伴对话——高优先
    PlayerEavesdropping = 2,    // 玩家旁听 NPC 对话——中优先
    L1Ambient = 3,              // L1 环境对话——低优先, 30% 概率
    Background = 4,             // 异步批量(背景故事/日记)
}
```

---

## 十、对话取消/中断全时机

12 种结束原因：NaturalExhaustion / Farewell / SilencedToEnd / CombatStarted / UrgentThirdParty / EnvironmentalEmergency / DistanceDrifted / ActivitySilenced / PlayerCancelled / NpcGoalOverride / NpcEmotionalExit / LlmTimeout(fallback)

中断后：CombatStarted→保存快照("刚才说到一半——")；DistanceDrifted(>50m)→保存快照可能过期；ActivitySilenced(Sneaking)→立即静音

---

## 十一、安全边界

- LLM 永远不能直接修改游戏状态——只能产出文本
- ActionLibrary 封闭——LLM 只能选预定义行动 ID
- EntityValidator——引用必须存在
- PersonalityValidator——不违反核心人格
- ContentFilter——拦截不当内容
- 全部 LLM 输出记录日志
- 模板永远作为 fallback——LLM 失败/超时/被拒绝→无缝回退

---

## 十二、补充实现细节

### 12.1 共享基础设施（完整定义）

```rust
pub struct TokenBudget {
    pub daily_limit: u32,              // 玩家设置, 默认 50000
    pub used_today: u32,
    pub warning_threshold: f32,        // 0.8 = 用掉80%时警告
    pub hard_limit_behavior: BudgetExhaustion,  // Block | WarnOnly
}
impl TokenBudget {
    pub fn can_afford(&self, estimated_tokens: u32) -> bool;
    pub fn reserve(&mut self, estimated_tokens: u32) -> BudgetReservation;
}

pub struct ResponseCache {
    cache: LruCache<u64, LlmCachedResponse>,
    max_entries: usize,           // 默认 500
    ttl_game_minutes: u32,
}

pub struct FallbackCoordinator;
impl FallbackCoordinator {
    pub fn decide(&self, request: &LlmRequest, ctx: &EnhancementContext) -> LlmDecision;
}
pub enum LlmDecision { Skip, UseCache(LlmCachedResponse), UseLlm(LlmRequest) }

pub struct TokenUsage { pub prompt_tokens: u32, pub completion_tokens: u32, pub total_tokens: u32 }

pub enum LlmAvailability { Available, Disabled, NoApiKey, SceneDisabled, Offline, RateLimited, BudgetExhausted }
```

### 12.2 NPC 对话意愿（完整公式）

```rust
// 个性驱动:
let personality = openness * 0.30 + extraversion * 0.20 + agreeableness * 0.10;

// 情绪驱动:
let arousal_penalty = (arousal - 0.5).abs() * 0.30;
let pleasure_bonus = pleasure.max(0.0) * 0.10;
let mood = (0.15 - arousal_penalty + pleasure_bonus).max(0.0);

// 关系驱动 (连续, 非阈值):
let relationship = trust * 0.20;

// 话题驱动:
let topic_interest = match ... {
    source_memory => memory.impact_score * 0.25,
    Knowledge+expertise => 0.20,
    Gossip => extraversion * 0.15,
    JournalReference => 0.10,
    _ => 0.05,
};

// 情境驱动:
let mut situation = 0.20;  // 基础
if is_busy { situation -= 0.15; }
if in_danger { situation -= 0.20; }
if travel_companion { situation += 0.10; }
if gathering { situation += 0.05; }

// derive_speaking_rules 含敬语:
if culture.has_honorifics() {
    rules.push("- Use honorifics appropriate to interlocutor's age and status.");
}
```

### 12.3 旅伴对话（完整类型定义）

```rust
pub enum TravelUtteranceStyle {
    Muttering,            // 自言自语——不期待回应
    ToGroup,              // 对全体说——"前面有座桥"
    ToSpecific(EntityId), // 对特定人说
    ContinuingThread,     // 顺着前一个人的话说
}

pub enum SharedActivity {
    Walking, Climbing, Riding, Resting, Fighting, Sneaking,
    Gathering, Navigating, Swimming, Waiting,
}
impl SharedActivity {
    pub fn allows_speech(&self) -> bool {
        !matches!(self, SharedActivity::Fighting | SharedActivity::Sneaking)
    }
}

struct SpeakUrge { entity: EntityId, urge: f32, topic: ConversationTopic, style: TravelUtteranceStyle }

fn npc_interest_in_rumor_type(npc: &NpcSnapshot, rumor: &PropagationChain) -> f32 {
    let mut interest = 0.3;
    if rumor.involves_acquaintance_of(npc) { interest += 0.3; }
    if rumor.relevant_to_profession(npc.profession) { interest += 0.25; }
    if rumor.location_is_familiar_to(npc) { interest += 0.2; }
    interest += npc.personality.openness * 0.1;
    interest.min(1.0)
}
// 非LLM模式必须工作: multi_travel_turn() 不依赖 LLM, 始终产出 ConversationTopic, TopicResolver 用模板生成文本
```

### 12.4 灵活可闻半径（完整枚举）

```rust
// 环境噪音 5 级: Silent(1.5), Quiet(1.2), Moderate(1.0), Loud(0.6), Deafening(0.3)
// 地形 10+ 种: OpenPlain(1.3), Hillside(1.1), Forest(0.7), Canyon(1.5), Cave(1.4),
//              CityStreet(0.8), IndoorRoom(0.9), IndoorHall(1.2), Market(0.5), Temple(1.3)

pub struct EffectiveAudibleRadius { pub radius_m: f32, pub breakdown: RadiusBreakdown }
pub struct RadiusBreakdown { pub base: f32, pub env: f32, pub weather: f32, pub culture: f32, pub personality: f32, pub intentional: f32 }

pub fn eavesdropping_clarity(distance_m: f32, radius: f32) -> f32 {
    if distance_m <= radius { 1.0 - (distance_m / radius) * 0.2 }
    else { (0.7_f32).powf((distance_m - radius) / radius) }
}
```

### 12.5 沉默模型（完整类型定义）

```rust
pub struct SilenceTracker { pub duration_seconds: f32, pub context: SilenceContext, pub waiting_for: EntityId }
pub struct SilenceContext { pub last_speaker: EntityId, pub utterance_type: UtteranceType, pub response_expected: bool }
pub enum UtteranceType { Question, Statement, Emotional, Phatic, Command, SelfTalk }
pub enum PlayerSilenceType { Thinking, PlayerBusy, PlayerDistracted, PlayerPossiblyAfk, NormalTravelSilence }
```

### 12.6 NPC间对话——分级速率限制

```rust
impl LlmRateLimiter {
    pub fn can_use_with_priority(&self, priority: LlmPriority) -> bool {
        match priority {
            LlmPriority::PlayerInteractive => self.player_daily < 20 && self.concurrent < 2,
            LlmPriority::PlayerTravelCompanion => self.player_daily < 20 && self.concurrent < 2,
            LlmPriority::PlayerEavesdropping => self.player_daily < 14 && self.concurrent < 1 && !self.player_awaiting,
            LlmPriority::L1Ambient => self.concurrent == 0 && !self.player_awaiting && fast_rand() < 0.3,
            LlmPriority::Background => self.concurrent == 0 && self.idle_period,
        }
    }
}
```

### 12.7 对话取消/中断（完整类型定义）

```rust
pub enum ConversationCloseout {
    Normal,                           // 正常结束·播Closing Phatic
    Interrupted { snapshot: ConversationSnapshot },  // 可恢复
    Faded,                            // 旅伴渐远
    Paused,                           // 活动暂停后可恢复
    InstantSilence,                   // 潜行→立即收声
    ContinuingWithFallback,           // LLM超时→模板继续
}
// CombatStarted→保存快照("刚才说到一半——")·超游戏内1小时→快照过期作废
```

### 12.8 成本与延迟

| 指标 | 值 | 说明 |
|------|-----|------|
| 每游戏日调用量 | 5-38 次（取决于场景开关） | 玩家可控 |
| Token/游戏日 | ~10K-50K | — |
| 成本/游戏日 | ~$0.20 (GPT-4o 参考) | 本地模型=零费用 |
| 成本/月 | ~$6.00 | 玩家可设硬上限 |

| 场景 | 可接受延迟 | 推荐后端 |
|------|----------|---------|
| 深度对话 | <3s | 云端高质量 |
| 普通对话/旅伴 | <2s | 本地快速 |
| 战斗对话 | <1s | 本地极快 |
| NPC间对话 | <5s | 本地 |
| 背景故事/日记 | 异步 | 云端最高质量 |

### 12.9 LlmOrchestrator——统一入口

```rust
pub struct LlmOrchestrator {
    config: LlmSceneConfig,
    registry: LlmBackendRegistry,
    budget: TokenBudget,
    cache: ResponseCache,
    safety: SafetyGateway,
    fallback: FallbackCoordinator,
    prompt_lib: PromptLibrary,
    // 五大引擎
    dialogue: LlmDialogueEngine, text: LlmTextEngine,
    narrative: LlmNarrativeEngine, character: LlmCharacterEngine, world: LlmWorldEngine,
}
impl LlmOrchestrator {
    pub fn is_active(&self) -> bool { self.config.master_switch && self.registry.has_healthy_backend() }
    pub fn enhance_dialogue(&self, ...) -> NpcResponse { if !self.is_active() || !self.config.deep_dialogue { return template; } /* LLM flow */ }
}
```
- 模板永远作为 fallback

---

> **关联**: [[001-语言表达系统总纲]] · [[003-文本生成引擎]] · [[005-对话系统-核心机制]] · [[009-信息传播系统]] · [[010-非语言表达与语言联动]] · [[012-语音输出接口]] · [[../NPC活人感模块/NPC活人感开发文档ver2.0]]（LlmEnhancer stub）· [[../技术栈方案/001-WoWorld正式技术栈方案v3|技术栈方案 v3.0]]
