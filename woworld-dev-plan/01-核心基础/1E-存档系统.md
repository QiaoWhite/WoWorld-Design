# 1E — 存档系统

> **状态**: 🔴 未开始
> **所属阶段**: Phase 1 — 核心基础
> **前置依赖**: 1A ✅（SaveableModule trait）

## 目标

实现 LMDB 持久化层——`SaveableModule::snapshot_dirty() → SaveSystem → LMDB` 成为项目唯一持久化路径。世界生成 P13 输出纯内存 `ValidatedWorldState`，不直接写磁盘。

## 涉及模块

| 模块 | 设计文档 | 代码 |
|------|---------|------|
| 存档系统 | `WoWorld-Design/Happy Game/开发阶段/存档系统/`（7 篇·v2.0 CHG-055/056） | 新建 crate: `woworld_save` |

## 任务清单

- [ ] 创建 `woworld_save` crate（依赖 woworld_core + lmdb）
- [ ] 实现 `SaveSystem` — LMDB 环境管理（单文件 + 多 named_db）
- [ ] 实现 `SaveableModule` trait 的 LMDB 序列化
- [ ] 全量快照（snapshot） + 脏数据增量（snapshot_dirty）
- [ ] 存档版本管理（schema version + 向前兼容）
- [ ] 集成到 Godot（存档/读档 UI 调用 Rust）
- [ ] 确定性验证：存档→读档→世界一致

## 🧪 研究事项

| 问题 | 级别 | 状态 |
|------|------|------|
| LMDB Rust 绑定选择（lmdb-rs vs heed vs lmdb-rkv） | 🟡 | 🔴 待研究 |
| 存档 schema 设计（哪些数据属于哪个 named_db） | 🟠 | 🔴 待研究——需审阅存档系统设计文档 |
| 增量快照的 dirty flag 传播机制 | 🟡 | 🔴 待研究 |

## 验收标准

### 🔧 技术
- [ ] `cargo test -p woworld_save` 全部绿
- [ ] LMDB 读写性能：全量快照 < 1s（100MB 级别数据）
- [ ] 存档文件大小合理（不无限增长）

### 📐 设计
- [ ] SaveableModule trait 实现与 CLAUDE-INTERFACES.md 一致
- [ ] 世界生成 P13 通过 SaveSystem 持久化（不直接写磁盘）

### 🎮 功能
- [ ] Godot 中存档/读档正常
- [ ] 存档→重启 Godot→读档→世界一致（确定性验证）

## 预估工作量

2-3 冲刺
