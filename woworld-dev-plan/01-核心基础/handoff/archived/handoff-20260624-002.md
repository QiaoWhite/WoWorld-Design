# 交接文档 — CHG-064 偏离修复完成

> **日期**: 2026-06-24
> **编号**: handoff-20260624-002
> **上接**: handoff-20260623-001（元冲刺·宪法建立）
> **工作日志**: `work-logs/001-CHG064-偏离修复-20260624/work-log.md`

## 本次完成

### CHG-064 四偏离修复

| 编号 | 描述 | 状态 |
|------|------|------|
| D01 | `time_manager.gd` 重复 Rust 日循环逻辑 | ✅ 修复——`process()` 直接操控 Godot 节点 |
| D02 | `woworld_core` serde 未 feature-gate | ✅ 修复——optional feature |
| D03 | `WorldClock` 未使用 `_process()` | ✅ 已解决——`process(&mut self)` 正常运作 |
| D04 | `SubMaterialWeight` 死代码 | ⬜ 非偏离——设计文档规定的后续迭代功能 |

### 架构重构

- **大气驱动源**：`day_progress` → `sun_elevation`（物理同步）
- **锚点**：命名字段+硬编码 → `Vec<AtmosAnchor>` 数组（TOML 自由增删）
- **WorldClock**：`woworld_worldgen → woworld_core`
- **新 crate**：`woworld_atmosphere`

## 当前状态

```
woworld/ (Rust workspace, 5 crates)
├── woworld_core       (时间权威: WorldClock + WorldTime, 4 spatial traits, 21 SurfaceMaterial)
├── woworld_atmosphere (大气曲线: AtmosCurve 太阳高度角驱动, ResolvedAtmosphere 35 floats)
├── woworld_worldgen   (世界生成: 双层Perlin + BiomeClassifier, 依赖 core[serde])
├── woworld_spatial    (空间索引: GridEntityIndex + DdaVisibility + RingEventBus)
└── woworld_godot      (GDExtension 桥接: TerrainChunk, 直接操控Godot节点)

57 tests, clippy zero warnings.
Godot 4.7 编辑器可运行: 地形渲染 + 日循环 + WASD漫游。
```

## Godot 启动

```bash
cd woworld
../tools/godot/Godot_v4.7-stable_win64_console.exe godot/project.godot
```

## 下一步建议

1. **DEVELOPMENT_STATUS.md 更新**（严重过时）
2. **宪法 §14 合规审计**（三层扫描）
3. **`session-handoff.md` 清理**（根目录旧格式，建议归档）
4. **下一个设计模块补全**（名声/法律/UI·UX 扩展）
5. **轨A 阶段三**：体素 Transvoxel + Clipmap LOD

## 关键约定

- **GDScript 铁律**（宪法 §14.1）：`.gd` 文件每行只能是 ①读 Input ②读 Rust `#[func]` ③设 Godot 节点属性。数学公式/状态机/游戏世界状态分支一律禁止。
- **TOML 单一真理源**：锚点数据只存在于 TOML 文件中，Rust 代码从 TOML 派生运行时参数，不硬编码。
- **工作日志**：每次开启新对话时写工作日志到 `woworld-dev-plan/work-logs/NNN-描述-YYYYMMDD/`。
- **交接归档**：旧交接移至 `handoff/archived/`，不删除。
