# 会话交接 — 2026-07-06 (下半场: Sprints 056-057 ECS-Godot 可视化)

## 💾 恢复点

```bash
cd woworld
cargo check --workspace   # 5 crates, 零错误
cargo test --workspace    # 332/332 ✓
cargo clippy --workspace -- -D warnings  # 零警告
cargo build --workspace   # ✅ DLL 已更新
```

## 本次提交 (10 个)

| # | 提交 | 内容 |
|---|------|------|
| 056 | `ce8154b` | 清理 Sprint: PRNG 提取 + 命名统一 + Wander 死代码移除 |
| 057 | `ed4e826` | ECS-Godot 可视化桥接 Phase A-C |
| — | `a6bf0d2` | fix: set_global_position 移到 add_child 之后 |
| — | `b349db8` | fix: NPC/Player spawn Y 从地形查询 |
| — | `2c67054` | fix: spawn 分步 insert + unshaded 材质 |
| — | `d601f99` | fix: 地形跟随 terrain clamp |
| — | `8b961b8` | fix: 胶囊体高度 offset 修正 |
| — | `f501d90` | fix: root 节点放地表 + mesh 偏移 |
| — | `5e47685` | diagnostic: BoxMesh 1×2×1m + 首帧日志 |
| — | `298b7d9` | fix: 颜色 splitmix hash + 禁用 Voxel 日志 |

## Sprint 056: 清理 (1 提交)

- `woworld_ecs/src/prng.rs`: 共享伪随机模块 (splitmix64 变体)
  - `pseudo_random_f32(seed)` / `_salted(seed, salt)` / `_range(seed, salt, min, max)`
  - 消除 bigfive/aesthetic/gender/movement 4 处重复
- `SocialPresence::from_bigfive` → `derive_from_bigfive` (命名统一)
- `ActionCategory::Wander` 变体 + 死分支移除

## Sprint 057: ECS-Godot 可视化 (9 提交)

### Phase A: 接入全部 15 ECS System
- `update_sun_and_sky()` 新增 `delta: f64` 参数
- 7 个缺失系统接入: movement, emotion_drift, physiological_pull, social, bigfive_derive, action_weight, age
- 按 borrow 约束分 4 组执行 (A1: &mut World → A2: movement → A3: age → A4: CommandBuffer batch)

### Phase B: NPC spawn 管线
- `WorldDriver::spawn_npc(seed, position)` — 20-Component 全量 bundle
- `ready()` 中 spawn 20 NPC，半径 30m 环形分布
- Player ECS entity spawn Y 从 terrain.height_at 查询

### Phase C: EntityRenderer 视觉桥接
- 新文件 `entity_renderer.rs`: ECS Position → Godot Node3D 每帧 sync
- 当前使用 BoxMesh (1×2×1m) 占位 + StandardMaterial3D unshaded
- splitmix-hash 颜色每人不同
- 地形跟随: 每帧 terrain clamp (Phase 1 临时方案)
- "Entities" 容器节点挂在 terrain_parent 下

### 已知问题与待办

| 项 | 状态 |
|----|------|
| 地形跟随 Y 钳制 | Phase 1 临时方案 — Phase 2 应改为 terrain-aware movement system |
| BoxMesh 占位 | 确认渲染正常后可换回 CapsuleMesh |
| LOD0 黑色方块回归 | 用户反馈已重现，未排查——VoxelChunk 渲染代码未被本 Sprint 改动 |
| 方块半截入土 | 斜坡正常——1m 宽方块在坡度处高侧入土低侧悬空 |

## 项目状态

| 指标 | 值 |
|------|-----|
| Crate | 5 (core, worldgen, atmosphere, ecs, godot) |
| Tests | **332** |
| ECS Component | 35 |
| ECS System | 20 |
| ECS System 已接入 Godot 主循环 | **15/15 (100%)** |
| NPC 可视化 | ✅ 21 彩色方块 (1 Player + 20 NPC) |
| NPC 行为 | 漫步 (wander) — Goal 存在但 target_pos 为 None |

## 下一步建议

| 方向 | 内容 |
|------|------|
| **LOD0 黑色方块** | 用户 report 回归，建议先排查 |
| **Gompertz 死亡** | 生命周期 Phase 2 — 年龄 >70% 后月度死亡判定 |
| **社交深度** | 情绪传染、关系记忆、对话雏形 |
| **terrain-aware movement** | 将 TerrainQuery 注入 movement_system 替代每帧 clamp |
| **NPC 可视化优化** | BoxMesh → CapsuleMesh，加 billboard 名字标签 |

## 关键架构决策速查

- **15 System 执行顺序**: needs_decay → regen → emotion_drift → physio_pull → social → movement → age → derive → need_eval → goal_res → action_weight → death_watch → loot_roll → item_spawn → corpse_decay → cleanup
- **terrain clamp**: Phase 1 在 process() 末尾硬钳制 Position.y = terrain.height_at(x,z)
- **mesh 偏移**: root 放地表 (y=terrain)，MeshInstance3D 子节点局部偏移 (y=half_height)
- **EntityRenderer 模式**: HashMap<hecs::Entity, Gd<Node3D>> — 创建/更新/移除 三态
