# 会话交接 — 2026-07-08

## 💾 恢复点

```bash
cd woworld
cargo check --workspace   # 5 crates, 零错误
cargo test --workspace    # 737/737 ✅
cargo clippy --workspace -- -D warnings  # 零警告
cargo build --workspace   # ✅ DLL 已更新
```

## 本次交付

### 物品系统 Phase 2（上午）

| 交付 | 详情 |
|------|------|
| 核心类型 | PersonalInventory/InventorySlot/InventoryError；OutfitSet/AccessorySet/ContainerSet/CharacterEquipment/SlotId/OutfitMode/EquipmentVisualToggles；ItemAssembly/JointType/ParamDef/ParamSchema stub |
| InventoryRegistry | HashMap SoA 存储，CRUD + seed_npc_items + container_bonus + equip/unequip |
| ECS 组件 | HasInventory/HasEquipment ZST（0B, Copy） |
| ECS 系统 | inventory_init_system — NPC 库存初始化 + 初始物品播种 |
| inventory_tuning | 所有 TOML 可调常量（BASE_SLOTS=30, MAX_THEORETICAL_SLOTS=215） |
| 集成 | WorldDriver +inventory_registry 字段，Block A4 调用 inventory_init_system |
| 架构决策 | InventoryRegistry 为库存权威源；EconomyRegistry.item_holdings 共存（Phase 3 迁移）；ItemDefId 替代 ItemEntId；复用现有 Wallet |

### 经济系统 Phase 3（下午）

| 交付 | 详情 |
|------|------|
| 需求类型 | NeedCategory(Physiological/Occupational/Social) + Urgency(Critical/High/Normal/Low) + NeedReason(Hunger/Thirst/CraftingIngredient/Gift/LuxuryDesire) |
| 挂单类型 | ListingType(Normal/Urgent{premium_pct,auto_accept}/Passive) + ListingStatus(Active/PartiallyFilled/Filled/Cancelled/Expired) + urgency_to_listing_type |
| Partial fill | match_orders 降量而非全删；filled_quantity 追踪；PartiallyFilled 状态保留订单 |
| Scarcity bonus | calculate_scarcity_bonus — bid_vol/ask_vol ratio→[-0.15, +0.30] |
| Bootstrap | BootstrapParams/initial_money_supply(M0=GDP/v)/inject_liquidity |
| Needs 连接 | assess_physiological_needs — Vitals.hp/spirit→食物 urgency→买单 |
| Order 增强 | listing_type/filled_quantity/status 三新字段 + Order::new() 构造函数 |

## 文件变更总览

### 新建文件 (10)

```
woworld_core/src/item/inventory.rs              ← PersonalInventory + InventorySlot
woworld_core/src/item/equipment.rs              ← OutfitSet/AccessorySet/ContainerSet + CharacterEquipment
woworld_core/src/item/assembly.rs               ← ItemAssembly/JointType/ParamDef stub
woworld_core/src/item/inventory_tuning.rs       ← TOML 可调常量
woworld_ecs/src/components/inventory.rs         ← HasInventory/HasEquipment ZST
woworld_ecs/src/resources/inventory_registry.rs ← InventoryRegistry SoA 存储
woworld_ecs/src/systems/item/inventory.rs       ← inventory_init_system
woworld_core/src/economy/listing.rs             ← NeedCategory/Urgency/ListingType/ListingStatus
woworld_core/src/economy/bootstrap.rs           ← BootstrapParams/inject_liquidity
woworld_ecs/src/systems/economy/needs.rs        ← assess_physiological_needs
```

### 修改文件 (11)

```
woworld_core/src/item/mod.rs           ← +4 pub mod
woworld_core/src/economy/mod.rs        ← +2 pub mod; Order 3新字段 + Order::new()
woworld_core/src/economy/behavioral.rs ← +calculate_scarcity_bonus
woworld_core/src/lib.rs                ← prelude 新增
woworld_ecs/src/components/mod.rs      ← +pub mod inventory
woworld_ecs/src/resources/mod.rs       ← +pub mod inventory_registry
woworld_ecs/src/resources/economy_registry.rs ← partial fill 撮合增强
woworld_ecs/src/systems/item/mod.rs    ← +pub mod inventory
woworld_ecs/src/systems/economy/mod.rs ← +pub mod needs
woworld_godot/src/terrain_chunk.rs     ← +inventory_registry + inventory_init_system
```

## 关键接口速查

```rust
// ── 库存 (woworld_core::item::inventory) ──
PersonalInventory::new(capacity) -> Self
inv.add(item_def_id, quantity, &props, max_weight_grams) -> Result<u32, InventoryError>
inv.remove(slot_idx, quantity) -> Result<u32, InventoryError>
inv.count_item(item_def_id) -> u32

// ── 装备 (woworld_core::item::equipment) ──
eq.set_slot(slot, mode, item) -> Option<ItemDefId>  // 返回旧装备
eq.get_slot(slot, mode) -> Option<ItemDefId>

// ── InventoryRegistry ──
reg.add_item(entity, def_id, qty, &item_registry) -> Result<u32, InventoryError>
reg.count_item(entity, def_id) -> u32
reg.get_holdings(entity) -> Vec<(ItemDefId, u32)>
reg.seed_npc_items(entity, &item_pool, seed, &item_registry)
reg.equip_to_slot(entity, slot, def_id) -> Result<Option<ItemDefId>, InventoryError>
reg.unequip_from_slot(entity, slot) -> Option<ItemDefId>

// ── 需求评估 (woworld_ecs::systems::economy::needs) ──
assess_physiological_needs(&vitals, &item_registry) -> Vec<NeedAssessment>
assess_all_needs(Option<&Vitals>, &dyn ItemQuery) -> Vec<NeedAssessment>

// ── 经济新类型 ──
urgency_to_listing_type(urgency) -> ListingType
calculate_scarcity_bonus(bid_count, ask_count, bid_vol, ask_vol) -> f32
initial_money_supply(&BootstrapParams) -> u64
inject_liquidity(&[ItemDefId], &dyn Fn, multiplier, seed) -> Vec<LiquidityInjection>
```

## 下一步建议

| 方向 | 内容 |
|------|------|
| **可视化** | NPC CapsuleMesh + 名字标签 + billboard（21 NPC 不可见） |
| **对话雏形** | NPC-NPC 基础对话模板 |
| **玩家系统** | 夺舍 NPC + 控制切换（CHG-063 设计就位） |
| **物品 Phase 3** | ItemEntId 迁移 + EconomyRegistry 完全去重 |
| **经济 Phase 4** | 多市场 + ProfessionTag 驱动 + 行为经济学全量接入 |

## ⚠️ 已知简化

- ListingType 采用 004 版本（Normal/Urgent{payload}/Passive），002 的 7 变体延后
- ListingStatus::Cancelled 无 payload 字段（Phase 4）
- assess_occupational_needs/social_needs 为 stub
- EconomyRegistry.item_holdings 与 InventoryRegistry 共存（Phase 3 未完全去重）
- 无多市场/区域选择
- 无 ProfessionTag 驱动
