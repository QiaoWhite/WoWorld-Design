# 交接摘要 — 2026-06-25 (Sprint-006 + 007)

## Sprint-006: 地基修复（✅ 完成）

| 目标 | 验收 | 状态 |
|------|------|------|
| 🔴1 DensityField trait 补全 | trait 含 sample/material_at/priority + height_at() 可选钩子 | ✅ |
| 🔴2 Seed u32→u64 + hash 派生 | WorldNoise::new(u64), mix64, hash_stage_seed, hash_chunk_seed | ✅ |

**核心架构决策**：
- `material_at() -> u8`（007 §2 体素材质 ID，不是 SurfaceMaterial）
- `HeightfieldDensity` 自持 `BiomeClassifier`，原生确定 u8
- `extract_isosurface(&dyn DensityField)` — 纯 3D 采样 + 2D 高度缓存快速路径
- `height_at() -> Option<f64>` 作为 trait 可选钩子——不强制纯 3D 层实现
- `mix64` 纯算术 hash——零依赖，永久跨平台确定性

**附带修复**：
- `ocean.gdshader`: vec3→vec4 uniforms + float d→vec2 d
- WorldDriver 对象池 100→256 + 一次性警告

## Sprint-007: 性能修复（✅ 完成）

| 目标 | 验收 | 状态 |
|------|------|------|
| poll() 帧预算 | 每帧最多生成 4 tile，多余推迟到待生成队列 | ✅ |
| 合并查询去冗余 | sample_vertex() 一次噪声调用得高度+法线+材质 | ✅ |
| 海洋深度着色 | 纹理方案未执行——判定为占位符抛光，转向植被 | ⏭️ 跳过 |

**核心架构决策**：
- `poll()` 新增 `pending_loads: Vec<LodKey>` 字段——永久正确，不依赖 Transvoxel
- `HeightfieldTerrain::sample_vertex()` 公开方法——永久基础设施
- 海洋着色判定为"占位符抛光"——不值得继续投资，海洋"能用就行"

**附带修复**：
- 空 mesh tile 跳过上传（消除 array_len==0 错误 ×8）
- OceanPlane 跟随玩家 + 放大到 2500m

## 当前 WIP 精确状态

### 代码侧
```
woworld/ Rust workspace
├── 5 crates, 87 tests 全绿, clippy 零警告
├── woworld_worldgen: 52 tests (seed 5 + noise_gen 9 + density 5 + MC 3 + terrain 8 + mesh 3 + chunk 9 + clipmap 6 + biome 5)
├── Godot F5 运行正常: 地形 MC + 4 级 Clipmap LOD + 海洋 + 昼夜 + 大气
└── 性能: 首帧分批加载 ~44 frames (~1.5s), 移动中偶有微卡
```

### 红色偏离状态
| # | 偏离 | 状态 |
|---|------|------|
| 🔴1 | DensityField trait 残缺 | ✅ Sprint-006 |
| 🔴2 | Seed u32 | ✅ Sprint-006 |
| 🔴3 | Chunk 128m vs 32m | 未修 — 阻塞存档对齐 |
| 🔴4 | MC vs Transvoxel | 未修 — 阻塞 LOD 无缝 |
| 🔴5 | 单层密度 vs 11 层 | 未修 — 阻塞洞穴/矿脉/SDF |

### 审计优先级进度
- P0: ✅ ✅ 全部完成
- P1: 植被渲染（推荐下一步）· 海洋系统（跳过——P1 数据结构更有价值）
- P2: 天气· 存档 — 未开始

## 下一步候选

| 候选 | D(×3) | R(×3) | V(×2) | C(×1) | H(×2) | 总分 |
|------|-------|-------|-------|-------|-------|------|
| **植被 P2.25 基础设施** | 4 | 4 | 3 | 3 | 4 | 40 |
| P1 海洋数据结构 | 3 | 3 | 2 | 4 | 3 | 32 |
| 🔴3 Chunk 32m | 5 | 2 | 0 | 5 | 5 | 37 |

> **推荐**: 植被 P2.25 — 解锁 P2.5/P3/P3.5 共 5 个下游模块。`VegetationProvider` trait 已有完整规格（7 方法），新建 `woworld_vegetation` crate，第一步仅需 Shannon 熵优势种筛选（~50 行，3 秒全量）。无视觉产出但架构价值最高。

## 已知问题

- [ ] 移动中偶有微卡（MC tile 生成仍为 4/frame，新区域加载需几十帧）
- [ ] 海洋全色统一（已知——占位符海洋，待 P1 海洋数据结构时统一解决）
- [ ] looking_at colinear 警告（Godot 内部阴影计算，无副作用）
- [ ] chunk_manager.rs 僵尸代码（保留但未使用，由 clipmap 替代）
- [ ] 宪法 v1.4 用户审批（自 v1.1 起待审批）

## 关键设计决策记录

1. **DensityField trait 4 方法**: sample + material_at + priority + height_at(可选) — grill-me 12 问全数闭环
2. **Seed 体系**: u64 seed → mix64 链 → 5×u32 Perlin seed。hash_stage_seed/hash_chunk_seed 作为独立公共 API
3. **MC 优化策略**: trait 加 height_at() 可选钩子（永久） → MC 内 2D 缓存快速路径（Transvoxel 时替换）
4. **对象池警告**: 改为一次性 godot_print——动态扩容是正常代码路径
5. **海洋着色**: 判定为占位符抛光——不值得在 shader 纹理上投资。P1 海洋数据结构才是正确的下一步
6. **下一冲刺方向**: 植被 P2.25 基础设施——架构解锁价值 > 视觉回报
